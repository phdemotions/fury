% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fury_execute_recipe.R
\name{fury_execute_recipe}
\alias{fury_execute_recipe}
\title{Execute a Recipe to Produce Audit Artifacts}
\usage{
fury_execute_recipe(recipe, out_dir = tempdir())
}
\arguments{
\item{recipe}{A \code{niche_recipe} object built by \code{vision::build_recipe()}.}

\item{out_dir}{Character scalar. Output directory for audit artifacts.
Defaults to \code{tempdir()}. Directory will be created if it does not exist.}
}
\value{
A \code{niche_result} object (defined by nicheCore) containing paths
to audit artifacts and execution metadata.
}
\description{
Takes a validated \code{niche_recipe} and produces audit artifacts
in the specified output directory.
}
\details{
This is the core execution function of fury. It creates output directories,
generates audit artifacts (source manifest, import log, codebook, session info),
and returns a \code{niche_result} object.
}
\examples{
\dontrun{
# Assuming you have a recipe
spec <- vision::read_spec("path/to/spec.yaml")
recipe <- vision::build_recipe(spec)
result <- fury_execute_recipe(recipe)
}
}
