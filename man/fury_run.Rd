% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fury_run.R
\name{fury_run}
\alias{fury_run}
\title{Run the Complete fury Workflow from a Spec File}
\usage{
fury_run(spec_path, out_dir = tempdir())
}
\arguments{
\item{spec_path}{Character scalar. Path to a spec file (YAML or JSON).}

\item{out_dir}{Character scalar. Output directory for audit artifacts.
Defaults to \code{tempdir()}. Directory will be created if it does not exist.}
}
\value{
A \code{niche_result} object (defined by nicheCore) containing audit
artifacts and metadata.
}
\description{
Novice-friendly entry point that reads a spec file, validates it,
builds a recipe, and executes data ingestion with audit artifacts.
}
\details{
This function orchestrates the full fury workflow:
\enumerate{
\item Reads and validates the spec via \code{vision::read_spec()} and \code{vision::validate_spec()}
\item Builds a recipe via \code{vision::build_recipe()}
\item Executes the recipe via \code{fury_execute_recipe()}
}
}
\examples{
\dontrun{
# Create a minimal spec
spec_path <- tempfile(fileext = ".yaml")
vision::write_spec_template(spec_path)

# Run fury workflow
result <- fury_run(spec_path)
print(result)
}
}
